version: '3.8'

services:
  surugaya-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:8080"  # HTTP
      - "5001:8081"  # HTTPS (如果有設定)
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080;https://+:8081
      # 根據需要添加其他環境變數
      # - ConnectionStrings__DefaultConnection=your_connection_string
    volumes:
      # 如果需要持久化資料或配置，可以在此添加 volumes
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# 如果需要資料庫，可以添加
# database:
#   image: postgres:15
#   environment:
#     POSTGRES_DB: surugaya
#     POSTGRES_USER: admin
#     POSTGRES_PASSWORD: password
#   ports:
#     - "5432:5432"
#   volumes:
#     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
